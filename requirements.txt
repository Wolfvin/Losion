# ============================================================================
# Losion v2.5.5 — Python Dependencies
# ============================================================================
# Pinned exact versions for reproducibility (audit finding C4.5).
# Update with care — major version bumps may break compatibility.

# Core
torch==2.5.1
numpy==2.2.3
einops==0.8.1

# Security — Authenticated encryption for episodic memory at rest
cryptography==44.0.1

# Configuration
pyyaml==6.0.2

# Training & Experiment Tracking
wandb==0.19.8
tensorboard==2.18.0

# Data Processing
datasets==3.3.2
tokenizers==0.21.0
transformers==4.48.3

# Evaluation
lm-eval==0.4.7

# Development
pytest==8.3.4
pytest-cov==6.0.0
mypy==1.15.0
ruff==0.9.7
pre-commit==4.1.0

# Optional — FP8 Training (uncomment for hardware support)
# torchao==0.8.0
# transformer-engine==1.13.0
